#further explanation of arrays on README file
#python doesn't have an built-in array data structure
#but we can use lists instead to represent them


#lists are ordered and allow duplicated values

values = [2, 3, 2, 8, 9]

#this function sums the total value of all values in an array
def find_values(values):
  total = 0
  while len(values) != 0:
    for i in values:
      total += i
    return total

print(find_values(values))

